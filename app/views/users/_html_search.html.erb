<p>The search results are:</p>

<% @selected.each do |photo| %>
	
	<div class="nailImg"> <%= link_to image_tag("#{photo.file_name}", :class => "nailImage"), {:controller => "photos", :action => "index", :id =>"#{photo.user.id}", :anchor => "img#{photo.id}" } %> </div>

<% end %>

